// Le contenu (pages, articles) est géré par Keystatic et lu via @keystatic/core/reader
// (voir src/lib/content.ts), pas par astro:content. On déclare donc zéro collection
// ici pour désactiver l'auto-détection dépréciée d'Astro sur src/content/.
export const collections = {};
